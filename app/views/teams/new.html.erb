<%= form_with model: @team, local: true do |f| %>

    <%= collection_check_boxes(:boss, :team_ids, Team.all, :id, :name) do |tag| %>
        <%= tag.label do %>
            <%= tag.check_box %>
            <%= tag.text %>
        <% end %>
    <% end %>
    <%= f.submit %>
<% end %>